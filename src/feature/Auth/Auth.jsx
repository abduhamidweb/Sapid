import React, { useState } from 'react';
import { Navigate } from 'react-router-dom';
import Routers from "../../hooks/routers/index"
const Auth = () => {
    // const [checkData, setCheckData] = useState(true);
    // let userData = JSON.parse(localStorage.getItem("userData"));
//     if (userData) {
//         setCheckData(true);
//         <Navigate to={"/"} />
//    }
    // userData ? setCheckData(true) : setCheckData(false)
    return (
        <div>
            {/* <Routers userData={ checkData} /> */}
        </div>
    );
};

export default Auth;